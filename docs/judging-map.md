# Judging map

| Dimension            | Evidence                                                                                                                                                                                                        |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Usefulness           | Directly reduces the trust gap for every agent marketplace entry; produces reusable evidence instead of another closed score. No account/key required for judges.                                               |
| Execution            | Real state machine, nine executed trials, SSE, deterministic scoring, evidence roots, Ed25519 verification/tamper demo, SSRF primitives, tests, responsive UI, Docker and machine-readable API.                 |
| Originality          | Separates identity from capability proof: “Orion verifies existence; AgentTrial proves performance.” Plan sealing prevents outcome cherry-picking. Browser-local verification removes trust in AgentTrial’s UI. |
| AI vetting readiness | Typed schemas, explicit permissions, untrusted-content isolation, full evidence links, coverage/confidence, honest failure and limitation states.                                                               |
| Community signal     | The launch plan asks builders to test and critique reproducible receipts, not trade votes or inflate metrics.                                                                                                   |

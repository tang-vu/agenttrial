# Optional. The controlled demo requires none of these.
OPENAI_API_KEY=
OPENAI_MODEL=
AGENTTRIAL_SIGNING_SEED=
NEXT_PUBLIC_APP_URL=http://localhost:3000
DATABASE_URL=
# Optional durable completed-run snapshots for a single-node deployment.
AGENTTRIAL_DATA_DIR=
AGENTTRIAL_RETENTION_DAYS=30
AGENTTRIAL_TRUST_PROXY=false

# Base Sepolia EAS (never expose private values with NEXT_PUBLIC_)
EAS_RPC_URL=https://sepolia.base.org
EAS_PRIVATE_KEY=
EAS_SCHEMA_UID=
EAS_EXPECTED_ATTESTOR=
EAS_ATTESTATION_ENABLED=false
AGENTTRIAL_TRUSTED_PUBLIC_KEY=
REPORT_URI=

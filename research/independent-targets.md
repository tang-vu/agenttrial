# P26-002 candidate source-unit lock

The candidate source-unit inventory contains 80 pinned units, ten provisionally assigned to each fault family. These are not yet validated configurations or statistically independent targets. Every source is public, pinned by commit and blob hash where applicable, and licensed MIT or Apache-2.0. No paid API or third-party service probing is required.

| Source                                                                                          | Selected | Primary role                                                                  | License    |
| ----------------------------------------------------------------------------------------------- | -------: | ----------------------------------------------------------------------------- | ---------- |
| [AgentChaosBench](https://github.com/kevinzck8k/agentic-fault-diagnosis)                        |       50 | Sanitized, aligned runtime-fault and no-fault traces across five systems      | MIT        |
| [AgentDojo](https://github.com/ethz-spylab/agentdojo)                                           |       10 | Prompt-injection tasks with upstream utility and security functions           | MIT        |
| [BFCL V4](https://github.com/ShishirPatil/gorilla/tree/main/berkeley-function-call-leaderboard) |       10 | Missing-parameter tasks with executable ground truth and matched base entries | Apache-2.0 |
| [tau2-bench](https://github.com/sierra-research/tau2-bench)                                     |       10 | Scoring-contract omission stress cases independently documented in issue 384  | MIT        |

`independent-targets.json` is the machine-readable source lock. It contains each source revision, selected ID, matched control, label authority, relevant upstream blob hashes, and release boundary.

## Source-lock controls

- Source revisions and labels were locked before adapter implementation or evaluator outcomes.
- Upstream ground-truth fields are prohibited from evaluator inputs.
- Adapters may translate schemas but may not change labels after results are observed.
- Two independent reviewers must approve construct mappings before any main run.
- Large upstream traces are not vendored. The public artifact contains identifiers, adapters, hashes, and derived aggregates.

## Construct limits

Three mappings remain deliberately narrow:

- AgentChaosBench `corruption` supports corrupted tool-output grounding, not a general claim about citation provenance.
- `agent_misroute` and `infinite_loop` are aligned routing/loop divergence proxies, not stochastic instability.
- tau2 issue 384 is an independently reported open audit. These entries are an evaluator-omission stress set, not a claim that maintainers have adjudicated the issue.

The first read-only adapter smoke test now passes against the pinned 203,596-byte AgentChaosBench trace for `ext-001`: 70 spans were projected, no forbidden key or locked label value leaked, and only the derived projection hash is retained in `targets/agentchaos-adapter-smoke.json`. The source trace is not vendored.

Source-schema smoke tests also pass for the three remaining source families. An upstream-published AgentDojo run was projected without utility, security, attack setup, or source identifiers. Pinned BFCL and tau2 source records were validated with synthetic candidate executions used only to exercise adapter paths; ground truth, reference paths, initial state, and evaluation criteria were excluded. Derived hashes are recorded in `targets/multi-source-adapter-smoke.json`, and no upstream source payload was retained.

The source-availability audit now verifies all 80 frozen units at their pinned revisions: 50 AgentChaosBench trace and control paths plus label blobs, 10 AgentDojo task/attack pairs against public run schemas, all 10 BFCL fault and control question/answer pairs, and all 10 tau2 task records against the frozen field extraction. `targets/source-availability-audit.json` publishes only identifiers, blob hashes, sizes, and derived checks. Upstream payloads and AgentDojo utility/security outcomes are not retained.

All 50 pinned AgentChaosBench traces passed the historical label-blinded adapter run. The audit processed 25,063,408 source bytes and 11,621 trace spans, produced 50 projection hashes, and found zero forbidden keys. Forty upstream records have a null `question`, so the adapter assigns a neutral task fallback while preserving the full observable telemetry. Natural words such as “corruption” remain observable evidence; only structural ground truth and unique locked identifiers are blocked. Because the audit does not retain canonical projection and source-execution bytes for gate-time recomputation, these hashes are inventory only and are excluded from readiness.

The ten AgentDojo paths were pinned in the availability audit using a fixed `command-r-plus` selection rule before their outcomes were inspected. The subsequent label gate found `security=false` for all ten upstream-published runs, so they provide ten independently labeled prompt-injection fault executions. All ten pass the label-blinded projection adapter with utility, security, attack setup, and source identifiers excluded from evaluator input. Four runs have `utility=true`; utility is reported separately and does not override the security failure. Derived hashes and upstream label summaries are recorded in `targets/agentdojo-projection-audit.json` without retaining run payloads.

Controlled candidate executions are still missing for the 10 BFCL and 10 tau2 targets. A deterministic local dry run can reconstruct 50 fixed-upstream fault candidates from immutable source revisions and Git blob SHAs: 40 genuine AgentChaosBench faults and 10 AgentDojo prompt-injection failures. Its large payload is deliberately unpublished and ignored by Git, so the receipt is non-evidence and the canonical gate still counts 0/80 readiness-eligible fault projections. The 10 AgentChaosBench `no_fault` traces reserved for artifact-tampering are also not promoted because the preregistered post-execution mutation operator has not been applied. All 80 target-specific matched-control projections remain missing; the two synthetic adapter candidates are not study evidence, and active external testing remains disabled.

`targets/control-execution-contracts.json` now predeclares the 20 missing AgentDojo and tau2 control contracts without claiming that an execution exists. The ten AgentDojo controls use the upstream `benchmark_suite_without_injections` function with a null attack argument, no injection task, empty injections, exact effective task and environment blobs, forced rerun, and utility success. The ten tau2 controls pin each task, database, policy, runner, and evaluator blob. They require both upstream reward 1 and the listed non-vacuous assertion, handoff, tracking-number, or arrival-date check. Reward 1 alone is explicitly ineligible because the pinned tau2 communication and NL-assertion evaluators auto-pass empty criteria. These are candidate method contracts and still require redesigned-method and human review.

The target-binding audit now makes the pre-run gap machine-readable instead of treating family membership as a complete mapping. It prepares 80 deterministic, provisional family-order bindings solely for review. None of the 80 fault projections has materialized gate evidence: all 60 historical hashes remain excluded and 20 controlled fault projections are missing. The local reconstruction receipt does not change those counts. No matched-control projection is ready. Sixty control bindings have pinned inputs, but they represent only 20 unique input references because 50 AgentChaosBench bindings reuse ten no-fault traces. Twenty controls remain condition-only. Every binding still requires two independent human construct reviews; only disagreements require adjudication. The audit therefore records `mainTrialAllowed=false` and cannot be used as a results artifact.

Canonical readiness files are `design-validity-audit.json`, `targets/target-binding-audit.json`, `targets/repeat-execution-inventory.json`, `targets/control-execution-contracts.json`, `targets/trusted-runner-policy.json`, `targets/remaining-projection-audit.json`, `targets/remaining-control-source-audit.json`, `governance/construct-review-packet.json`, `governance/method-freeze-approval.json`, and `governance/g3-approval.json`. Both evidence manifests and all human records remain pending. `targets/fixed-upstream-reconstruction-receipt.json` records only an unpublished local dry run and is explicitly not a readiness input. The repeat inventory currently resolves to 60 unique physical execution identities: 60 fault bindings, 50 control bindings backed by ten identities, 40 reused control bindings, and ten identities shared across fault and control conditions. Candidate declarations, receipts, and unexecuted contracts never count without materialized gate-side evidence verification. Fixed upstream evidence has a Git-blob-verified deterministic adapter path. Controlled evidence has a fail-closed Ed25519 trusted-runner verifier, but cannot be promoted without an actual attestation from a human-registered key. The ten variant contracts are machine-readable and distinct, while their construct mappings remain human-review gates. Evidence alone still does not resolve repetition, control-independence, source acquisition, or human-review blockers. Automated agents may regenerate machine audits and validate human records but may not overwrite or supply human decisions.
